#include <string>

#include <timer.h>

void work() {
    FunctionTimer;
    for (int i = 0; i < 100; i++) {
        std::string a;
        a.reserve(1024);
    }
}

int main() {
    work();
    generateRaport();
}